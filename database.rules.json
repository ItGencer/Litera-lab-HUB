{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth !== null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".write": "auth !== null && root.child('users').child(auth.uid).child('role').val() === 'admin'"
      }
    },
    "Works": {
      ".read": true,
      "$workId": {
        ".write": "auth !== null && (!data.exists() && newData.child('authorUid').val() === auth.uid || data.exists() && (data.child('authorUid').val() === auth.uid || root.child('users').child(auth.uid).child('role').val() === 'admin'))"
      }
    }
  }
}